# PASS 游戏教程

PASS 是一场在 8×10 棋盘上进行的足球卡牌对决。你控制己方全部三名球员，对方三名球员由 AI 控制；先攻入 3 球的一方获胜。

## 开始比赛

1. 选择由你控制的红队或蓝队。
2. 依次选择己方球员并点击己方半场空格，调整三人的开局位置。
3. 点击“开始比赛”。顶部黄色标记表示当前行动者，球员旁的足球标记表示公开持球者。

联机创建房间时还可以选择两种合作控制模式：

- 3v3 双人整队：两名玩家各控制一整队三名球员。
- 4v4 四人搭档：每队两名玩家，分别控制 1/3 号和 2/4 号球员。

你可以点击自己控制的球员查看其手牌，但不能查看另一名真人队友控制球员的手牌。轮到自己控制的球员时，手牌会自动切换到该球员。

## 回合与行动力

每个回合先自动抽 1 张牌。准备阶段，无球队员获得 2 点行动力，持球者获得 1 点。当前测试牌库只有三类行动卡，每张消耗 1 点行动力。

如果本回合尚未行动，可以选择“战术整备”：跳过出牌并额外抽 2 张牌。行动结束后进入弃牌阶段，进攻方牌上限为 5，防守方为 6；足球不占上限。

## 移动与传球

- Rock：横向或纵向移动，单次最多 3 格。
- Bishop：斜向移动，单次最多 2 格。
- Knight：卡面用两段路线绘出一次“日”字移动：先向上直走一段，再向右上斜走到落点。

选择手牌后点击 MOVE，再点击高亮目标。球员不能穿过任何人。移动路径经过落地足球时会拾球。

持球者可选择行动牌后点击 PASS。Rock 横纵传球最多 6 格，Bishop 斜向传球最多 4 格，Knight 仍为一次标准折线路线。路线会被任何球员挡住；最先挡路的是队友时，可以直接把球传给他。传到空格后，首名合法经过该格的球员拾球。

## 当前测试牌库

当前只生成 Rock、Bishop、Knight 各 13 张，共 39 张。

## 防守、进球与越位

防守方可以基础上抢：先选择并弃置自己一张非足球牌，再对一格内的对方持球者使用。随机抽中普通牌时，该牌也被弃置；抽中足球时则夺得球权。每次上抢消耗 1 点行动力，只要条件仍满足，同一回合可以重复使用。

球门位于棋盘两端 D、E 线之外，每队有两个场外球门格。球员不能移动进入球门，只能用合法 Pass 射门得分。

棋盘纵轴为 `1、2、3、4、5、6、7、8、9、X`；`X` 表示第10行，因此格子仍用两个字符表示。`c1、d1、e1、f1` 与 `cX、dX、eX、fX` 分别是两端禁区。防守方在本方禁区同时最多停留 1 人；进攻方可以进入对方禁区，但身处对方禁区时不能射门。球权转换造成暂时超员时，只在身处本方禁区的防守球员自己回合结束时检查；仍超员便判犯规，进攻方持球，双方恢复默认阵型并从中场开球。

只有比赛开始时，初始防守方每名球员额外补 1 张牌。进球、犯规或之后重新开球时不再补牌。

## 互动训练目标

互动教程的目标栏只显示本关最终结果，具体操作会在目标下方分步列出。训练按以下顺序进行：

1. **移动球员：** 将 R1 移动到 `e5`。先用 Rock 从 `e3` 移到 `g3`，再用 Bishop 从 `g3` 移到 `e5`。
2. **路线不能穿人：** 将 R1 移动到 `d4`。先用 Rock 尝试从 `e3` 移到 `e5`，观察 `e4` 的 R2 阻挡路线；再用 Bishop 从 `e3` 移到 `d4`。
3. **直接传给队友：** 将足球传到 `e6` 的 R2。
4. **队友也会挡球：** 先尝试传向 `e7` 的 R3，观察 `e5` 的 R2 挡住线路，再将足球传给 `e5` 的 R2。
5. **行动力与手牌：** 不使用手牌，完成一次战术整备。
6. **空位传球与接应：** R1 将球传到 `e5`；R2 用 Bishop 移动到 `d6` 并在途中拾球，再将足球传给 `d8` 的 R3。
7. **基础上抢：** B1 先弃 Bishop 上抢 `d3` 的 R1，再弃 Rock 上抢同一目标并夺得足球。
8. **禁区与射门：** 先用 Knight 尝试射向蓝方球门 D，观察 `cX` 的 B1 封锁路线；再用 Bishop 射入蓝方球门 E。
9. **完整进攻演练：** R1 将足球传给 `d8` 的 R2，再由 R2 射入蓝方球门 D。

## 画面提示

实线箭头表示移动，虚线表示传球。棋盘上方会显示最近行动及其公开结果；比赛记录只用于回看细节。桌面版按 `F1` 可随时打开本教程。
